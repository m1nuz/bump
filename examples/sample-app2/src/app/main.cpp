#include <iostream>

#include <libsum/sum.h>
#include <libmul/mul.h>

int main( int argc, char *argv[] ) {
    std::cout << "Sample App" << '\n';
    std::cout << "Sum " << sum( 2, 3 ) << '\n';
    std::cout << "Sum " << sum1( 2, 3 ) << '\n';
    std::cout << "Sum " << sum2( 2, 3 ) << '\n';
    std::cout << "Sum " << sum3( 2, 3 ) << '\n';
    std::cout << "Sum " << sum4( 2, 3 ) << '\n';
    std::cout << "Sum " << sum5( 2, 3 ) << '\n';
    std::cout << "Sum " << sum6( 2, 3 ) << '\n';
    std::cout << "Sum " << sum7( 2, 3 ) << '\n';
    std::cout << "Sum " << sum8( 2, 3 ) << '\n';
    std::cout << "Sum " << sum9( 2, 3 ) << '\n';
    std::cout << "Mul " << mul( 2, 3 ) << '\n';
    std::cout << "Mul " << mul2( 2, 3 ) << '\n';
    std::cout << "Mul " << mul3( 2, 3 ) << '\n';
    std::cout << "Mul " << mul4( 2, 3 ) << '\n';
    std::cout << "Mul " << mul5( 2, 3 ) << '\n';
    std::cout << "Mul " << mul6( 2, 3 ) << '\n';
    std::cout << "Mul " << mul7( 2, 3 ) << '\n';
    std::cout << "Mul " << mul8( 2, 3 ) << '\n';
    std::cout << "Mul " << mul9( 2, 3 ) << '\n';

    return 0;
}
