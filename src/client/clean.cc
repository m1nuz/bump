#include <string_view>

#include "context.h"

namespace app {

    namespace commands {

        auto clean_all( context& ctx ) -> bool {
            return true;
        }

    } // namespace commands

} // namespace app
