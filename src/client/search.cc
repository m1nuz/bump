#include <string_view>

namespace app {

    namespace commands {

        auto search_package( const std::string_view package_name ) {
            return false;
        }

    } // namespace commands

} // namespace app
